Submodule dmenu contains modified content
diff --git a/dmenu/config.def.h b/dmenu/config.def.h
index 1edb647..a355295 100644
--- a/dmenu/config.def.h
+++ b/dmenu/config.def.h
@@ -4,7 +4,8 @@
 static int topbar = 1;                      /* -b  option; if 0, dmenu appears at bottom     */
 /* -fn option overrides fonts[0]; default X11 font or font set */
 static const char *fonts[] = {
-	"monospace:size=10"
+	"JetBrainsMono Nerd Font Mono:size=10",
+	"Noto Color Emoji:size=10",
 };
 static const char *prompt      = NULL;      /* -p  option; prompt to the left of input field */
 static const char *colors[SchemeLast][2] = {
Submodule dwm contains modified content
diff --git a/dwm/config.def.h b/dwm/config.def.h
index 9efa774..3b1f3fa 100644
--- a/dwm/config.def.h
+++ b/dwm/config.def.h
@@ -5,7 +5,10 @@ static const unsigned int borderpx  = 1;        /* border pixel of windows */
 static const unsigned int snap      = 32;       /* snap pixel */
 static const int showbar            = 1;        /* 0 means no bar */
 static const int topbar             = 1;        /* 0 means bottom bar */
-static const char *fonts[]          = { "monospace:size=10" };
+static const char *fonts[]          = {
+	"JetBrainsMono Nerd Font Mono:size=10",
+	"Noto Color Emoji:size=10",
+};
 static const char dmenufont[]       = "monospace:size=10";
 static const char col_gray1[]       = "#222222";
 static const char col_gray2[]       = "#444444";
